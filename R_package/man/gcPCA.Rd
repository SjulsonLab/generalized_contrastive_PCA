% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcPCA.R
\name{gcPCA}
\alias{gcPCA}
\title{Fit a generalized contrastive PCA model}
\usage{
gcPCA(
  Ra,
  Rb,
  method = "v4",
  Ncalc = NULL,
  Nshuffle = 0,
  normalize_flag = TRUE,
  alpha = 1,
  alpha_null = 0.975,
  cond_number = 10^13
)
}
\arguments{
\item{Ra}{Matrix for condition A (\verb{n_a x p}).}

\item{Rb}{Matrix for condition B (\verb{n_b x p}).}

\item{method}{Character scalar selecting optimization variant.}

\item{Ncalc}{Optional number of gcPCs for orthogonal variants (\verb{*.1}).}

\item{Nshuffle}{Number of shuffles used to estimate a null distribution.}

\item{normalize_flag}{Logical; if \code{TRUE}, z-score and L2-normalize columns.}

\item{alpha}{Contrastive coefficient used by method \code{"v1"}.}

\item{alpha_null}{Quantile for null thresholding (reserved in current code).}

\item{cond_number}{Maximum allowed denominator condition number.}
}
\value{
An object of class \code{"gcPCA"} with loadings, scores, objective values,
and fit metadata.
}
\description{
\code{gcPCA()} fits dense generalized contrastive principal components from two
data matrices (\code{Ra} and \code{Rb}) measured on the same feature set.
}
\examples{
set.seed(1)
Ra <- matrix(rnorm(40 * 5), ncol = 5)
Rb <- matrix(rnorm(35 * 5), ncol = 5)
fit <- gcPCA(Ra, Rb, method = "v4", Ncalc = 3)
pred <- predict(fit, Ra = Ra)
str(pred$Ra_scores)

}
