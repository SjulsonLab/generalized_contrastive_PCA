% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_gcPCA.R
\name{sparse_gcPCA}
\alias{sparse_gcPCA}
\title{Fit a sparse generalized contrastive PCA model}
\usage{
sparse_gcPCA(
  Ra,
  Rb,
  method = "v4",
  Ncalc = NULL,
  normalize_flag = TRUE,
  Nsparse = NULL,
  Nshuffle = 0,
  lasso_penalty = exp(seq(log(0.01), log(1), length.out = 10)),
  ridge_penalty = 0,
  alpha = 1,
  alpha_null = 0.975,
  tol = 1e-05,
  max_steps = 1000,
  cond_number = 1e+13
)
}
\arguments{
\item{Ra}{Matrix for condition A (\verb{n_a x p}).}

\item{Rb}{Matrix for condition B (\verb{n_b x p}).}

\item{method}{Character scalar selecting optimization variant.}

\item{Ncalc}{Optional number of gcPCs for orthogonal variants (\verb{*.1}).}

\item{normalize_flag}{Logical; if \code{TRUE}, z-score and L2-normalize columns.}

\item{Nsparse}{Number of sparse gcPCs to estimate.}

\item{Nshuffle}{Number of shuffles passed to the dense gcPCA stage.}

\item{lasso_penalty}{Numeric vector of lasso penalties.}

\item{ridge_penalty}{Numeric ridge penalty.}

\item{alpha}{Contrastive coefficient used by method \code{"v1"}.}

\item{alpha_null}{Quantile for null thresholding (reserved in current code).}

\item{tol}{Convergence tolerance for sparse optimization.}

\item{max_steps}{Maximum number of sparse optimization iterations.}

\item{cond_number}{Maximum allowed denominator condition number.}
}
\value{
An object of class \code{c("sparse_gcPCA", "gcPCA")} containing sparse
loadings and training projections across penalties.
}
\description{
\code{sparse_gcPCA()} fits sparse gcPCA loadings over a path of lasso penalties.
}
\examples{
set.seed(1)
Ra <- matrix(rnorm(50 * 6), ncol = 6)
Rb <- matrix(rnorm(45 * 6), ncol = 6)
fit <- sparse_gcPCA(Ra, Rb, method = "v4", Nsparse = 2,
                    lasso_penalty = c(0.05, 0.1))
pred <- predict(fit, Ra = Ra)
str(pred$Ra_scores)

}
